# ── Asmodeus (103) — API ─────────────────────────────────
NODE_ENV=production
PORT=3000
DATABASE_URL="postgresql://aukani_user:password_seguro@10.10.20.5:5432/aukani_pos"
JWT_SECRET="cambia-esto-por-algo-aleatorio-min-32-chars"
JWT_EXPIRES_IN="8h"

# ── Behemoth (104) — DB (docker-compose.db.yml) ──────────
POSTGRES_USER=aukani_user
POSTGRES_PASSWORD=password_seguro
POSTGRES_DB=aukani_pos
